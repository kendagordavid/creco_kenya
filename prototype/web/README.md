# Frontend moved to repository root

The Next.js app now lives at the **repo root** (`app/`, `package.json`) so Vercel deploys without a custom Root Directory.

Local dev:

```bash
npm install
npm run dev
```

Backend (optional, for local Python API): see `prototype/backend/`.
